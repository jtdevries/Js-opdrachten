document.write("<h3>Oefening 4</h3>");

let tab = [-2, 1, 4];
let add = +2

function display(){
  let last_element = tab[tab.length - 1];
  let first_element = tab[tab.length - 3];
  alert(first_element+add);
  alert(last_element+add);
}
